{
  "name": "chantier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chantier — jeu de stratégie solo/idle hors ligne, distribué comme un fichier HTML autonome.",
  "config": {
    "build": "1"
  },
  "scripts": {
    "build": "node tools/build.js",
    "test": "node --test",
    "check": "node tools/build.js && node --test"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
